import { useState } from 'react';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { TagInput } from './TagInput';

<Meta title="Components/TagInput" />

export const Basic = () => {
  const [value, setValue] = useState(['work', 'personal', 'school']);
  return (
    <TagInput
      value={value}
      onChange={(newValue) => setValue(newValue)}
      options={['project X', 'project Y']}
      placeholder="Enter a new tag"
    />
  );
};

export const Colorized = () => {
  const [value, setValue] = useState(['work', 'personal', 'school']);
  return (
    <TagInput
      value={value}
      onChange={(newValue) => setValue(newValue)}
      options={['project X', 'project Y']}
      placeholder="Enter a new tag"
      colorize
    />
  );
};

export const CustomCopmpleteKeys = () => {
  const [value, setValue] = useState(['work', 'personal', 'school']);
  return (
    <TagInput
      value={value}
      onChange={(newValue) => setValue(newValue)}
      options={['project X', 'project Y']}
      placeholder="Enter a new tag"
      completeKeys={['+']}
    />
  );
};

# Tag Input

<Story name="Basic">
  <Basic />
</Story>

<Story name="With Colors">
  <Colorized />
</Story>

<Story name="Custom Complete Keys">
  <CustomCopmpleteKeys />
  <pre>* Enter a new tag and hit `+`</pre>
</Story>
