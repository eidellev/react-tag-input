import React from 'react';

interface Props {
  options?: string[];
  value: string[];
  className?: string;
}

export const TagInput : React.FunctionComponent<Props> = (props: Props) => {
  return <div></div>
}
